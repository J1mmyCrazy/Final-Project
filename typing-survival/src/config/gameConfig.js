// Configuration constants used across the game UI and logic.

export const LANE_POSITIONS = [100, 350, 500] // Y positions (px) for lane indices 0..2
export const MAX_COMBO = 25 // Maximum combo multiplier cap to prevent runaway scoring
